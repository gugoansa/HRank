👶 Explicación Nivel Bebé: El Detective de Elementos
Imagina que la página web es un cuarto lleno de juguetes (botones, campos de texto, imágenes). Tu misión como automatizador es decirle a Playwright (el Detective) dónde está el juguete que debe usar.

Un Localizador es la dirección exacta que le das al Detective.

Mal Detective (Sin Localizador): "Haz clic en el botón." (❌ Playwright no sabe cuál).

Buen Detective (Con Localizador): "Haz clic en el botón que está etiquetado como 'Enviar' y tiene el número de identificación 'login-btn'." (✅ Playwright lo encuentra sin dudar).

Si le das una dirección ambigua, Playwright podría hacer clic en el juguete equivocado, y tu prueba fallará.

💻 Tipos de Localizadores en Playwright (La Realidad)
En Playwright, siempre se usa el método page.locator() para empezar, y dentro de él, se usan diferentes tipos de selectores. Playwright recomienda priorizar los selectores basados en Roles y Texto antes de usar selectores basados en la estructura del HTML (como CSS o XPath).

1. 🥇 Localizadores Basados en Roles (¡La Mejor Opción!)
Estos le dicen al Detective qué tipo de elemento es, basándose en lo que ve el usuario o las herramientas de accesibilidad.

Tipo    	¿Qué le dices al Detective?	                                            Ejemplo de Uso
getByRole	"Busca un botón que se llama 'Enviar'."	                                page.getByRole('button', { name: 'Enviar' })
getByText	"Busca un elemento que contenga el texto exacto 'Finalizar Compra'."	page.getByText('Finalizar Compra')
getByLabel	"Busca el campo de entrada que tiene una etiqueta 'Usuario'."	        page.getByLabel('Usuario')

2. 🥈 Localizadores Basados en HTML (Selectores CSS)
Si los roles no funcionan, usamos la dirección específica que viene del código HTML. CSS es el más rápido y común.

Tipo	        ¿Qué le dices al Detective?	                                    Ejemplo de Uso
Por ID	        "Busca el elemento con el número de identificación 'myId'."	    page.locator('#myId')
Por Clase	    "Busca todos los elementos con la clase 'item-card'."	        page.locator('.item-card')
Por Atributo	"Busca el elemento que tenga el atributo data-testid='login'."	page.locator('[data-testid="login"]')

3. 🥉 Localizadores Basados en Ruta (XPath)
XPath es poderoso, pero generalmente es lento y frágil. Solo úsalo si no hay otra opción.

Tipo	¿Qué le dices al Detective?	                                         Ejemplo de Uso
XPath	"Ve a la cuarta div del tercer section y busca el primer botón."     page.locator('//div[3]/section[1]/button[1]')

🗺️ ¿Cómo Localizar Cosas? (El Proceso)
Para encontrar el mejor localizador en tu página:

Inspeccionar: Usa las Herramientas de Desarrollador de tu navegador (clic derecho -> Inspeccionar) para ver el código HTML del elemento que te interesa.

Buscar Identificadores: Primero, mira si el elemento tiene un atributo id (id="miBoton") o un atributo data-testid (que se agrega para la automatización). ¡Estos son los mejores!

Probar Roles: ¿Es un botón? ¿Es un link? Busca el texto que el usuario ve (name: 'Comprar').

Escribir el Código:

Ejemplo Práctico:
Si el HTML es:  <button id="submit-btn" class="main-btn" role="button">Enviar</button>
Podrías usar:
Localizador	        Código Playwright	                            Razón
getByRole (Mejor)	page.getByRole('button', { name: 'Enviar' })	Usa el texto que ve el usuario y el rol. Muy estable.
CSS ID	            page.locator('#submit-btn')	                    Usa el ID. Rápido y único.
CSS Class	        page.locator('.main-btn')	                    Menos ideal, puede haber otras clases iguales.